import React from 'react';
import styles from '../../styles/footer.module.css';

function Footer (props) {
    return (
        <div className={styles['footer']}>
            <h4 className="py-5">This is a place holder for footer</h4>
        </div>);
}

export default Footer;